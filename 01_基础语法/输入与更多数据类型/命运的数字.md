1.	`std::cin`：与`std::cout`相对应，cin负责从标准输入设备（通常是你的键盘）读取数据。它也位于`<iostream>`头文件中。

2.	`>>`（流提取运算符）：它的方向与`<<`相反。`cout << data`; 是把数据“放进”输出流，而 `cin >> variable`; 是从输入流中“提取”数据，并存放到变量中。箭头的方向形象地表示了数据的流向。

3.	程序的阻塞：当你执行到`std::cin >> slimeCount;`这一行时，程序会暂停，也就是“阻塞”，直到你从键盘输入了内容并按下回车键。这是程序等待外部输入的正常行为。
